# Local development defaults. Copy to `.env` and adjust — never commit real secrets.
# Used only by docker-compose.yml (service credentials, ports).
# Application runtime config lives in backend/.env (see backend/.env.example).

POSTGRES_DB=db_villa_dieng
POSTGRES_USER=villa
POSTGRES_PASSWORD=villa_dev_password
POSTGRES_PORT=5432

REDIS_PORT=6380

APP_PORT=8001
FRONTEND_PORT=5173
